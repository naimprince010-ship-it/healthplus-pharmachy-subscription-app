async function testExpand() {
    const url = 'https://medex.com.bd/generics/1/paracetamol';
    console.log(`Testing expansion for: ${url}`);

    // We assume the dev server is NOT running, so we'll test the logic directly by importing the route?
    // No, that's complex in Next.js. I'll just assume I can call the external Medex URL and see if the logic works.
}
